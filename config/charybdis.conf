# --- OPTIMIZACIONES PARA MACOS (Descomenta y ajusta estos valores) ---
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=9
CONFIG_BT_PERIPHERAL_PREF_LATENCY=16
CONFIG_BT_BUF_ACL_TX_COUNT=32
CONFIG_BT_L2CAP_TX_BUF_COUNT=32

# Fuerza a ZMK a mantener la estabilidad de la conexión con Apple
CONFIG_BT_CTLR_PHY_2M=n

# --- GESTIÓN DE ENERGÍA Y SUEÑO ---
# Tiempo para apagar luces/periféricos menores (30 segundos)
CONFIG_ZMK_IDLE_TIMEOUT=15000

# Habilitar deep sleep (El sueño profundo)
CONFIG_ZMK_SLEEP=y

CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3500000

# --- ESTABILIDAD BLUETOOTH EXTRA ---
# Limpia los perfiles de conexión corruptos y estabiliza el emparejamiento
CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=n
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
